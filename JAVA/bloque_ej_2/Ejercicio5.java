// Ejercicio 5. Escribe un programa que calcule el area de un rectangulo:

import java.util.Scanner;

public class Ejercicio5 
{
    public static void main(String[] args) 
    {
        // Creamos nuestro objeto scanner (sc) para introducir datos desde teclado:
        Scanner sc = new Scanner(System.in);

        // Pedimos al usuario que introduzca el largo y el ancho del rectangulo:
        System.out.println("Introduzca el largo del rectangulo (en centimetros): ");
        float largo = sc.nextInt();
        System.out.println("Introduzca el largo del rectangulo (en centimetros): ");
        float ancho = sc.nextInt();

        // Sacamos por pantalla el calculo del area del rectangulo:
        System.out.println;
    }
}